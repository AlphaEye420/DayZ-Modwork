## XML Instance Counter for MapGroupPos.Xml DayZ files 
### Creates a json from the count of each 'name' in the xml file
1. copy your mission folder's mapgrouppos.xml into a new directory with both the .bat and .ps1 files included
2. drag the xml onto the .bat file and it will create "object_count.json"